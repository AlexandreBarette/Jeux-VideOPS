name: First Workflow

on:
  push:

jobs:
  demo_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "Hello Github Actions!"

      - name: List files
        run: ls -la